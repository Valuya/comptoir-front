import { ShellColumnDirective } from './shell-column.directive';

describe('ShellColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellColumnDirective();
    expect(directive).toBeTruthy();
  });
});

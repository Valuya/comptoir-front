import { FilterContentDirective } from './filter-content.directive';

describe('FilterContentDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterContentDirective();
    expect(directive).toBeTruthy();
  });
});

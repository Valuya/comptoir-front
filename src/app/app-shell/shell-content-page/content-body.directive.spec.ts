import { ContentBodyDirective } from './content-body.directive';

describe('ContentBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentBodyDirective();
    expect(directive).toBeTruthy();
  });
});

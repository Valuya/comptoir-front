import { InplaceInputDirective } from './inplace-input.directive';

describe('InplaceInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InplaceInputDirective();
    expect(directive).toBeTruthy();
  });
});

import {InplaceOutputDirective} from './inplace-output.directive';

describe('InplaceOutputDirective', () => {
  it('should create an instance', () => {
    const directive = new InplaceOutputDirective();
    expect(directive).toBeTruthy();
  });
});

import {LocalizedEditDirective} from './localized-edit.directive';

describe('LocalizedEditDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalizedEditDirective();
    expect(directive).toBeTruthy();
  });
});
